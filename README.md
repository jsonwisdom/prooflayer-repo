# Prooflayer Repo

> **Status: DEPLOYMENT SCAFFOLD**
>
> This repository contains the planned structure for a ZTWS prooflayer deployment.
> It is not currently a live deployment surface.
>
> Registry address is `TBD_POST_DEPLOY`.
>
> **Canonical Anchor 001** is in `jsonwisdom/Welcome-to-JSONWISDOM`.
> See `docs/ANCHOR_001_BOUNDARY.md`.

Repository scaffold for a proof-oriented system prototype.

## Structure

- `contracts/`
- `script/`
- `gateway/`
- `notarizer/`
- `observer/`
- `deploy/`

## Current State

Initial repository structure and documentation scaffold.

See `docs/DEPLOYMENT_STATUS.md` for current deployment status.

## Boundary

This repo may contain deployment plans, manifests, and checklists.

It must not be treated as live deployment truth until real deployment artifacts are committed:

- registry address
- deployment transaction hash
- deployment block
- manifest CID
- contract ABI
- verification status

Rule: no ghost deployment.
